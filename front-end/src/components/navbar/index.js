const Navbar = () => {
    return (
        <nav className="navbar">
            <div className="links">
                <a href="/">Homepage</a>
                <a href="/login">Login</a>
                <a href="/signup">Sign-up</a>
            </div>
        </nav>
    );
}

export default Navbar;